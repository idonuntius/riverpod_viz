name: riverpod_viz_devtools_extension
description: DevTools extension UI for riverpod_viz.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.5

dependencies:
  devtools_extensions: ^0.5.1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
